# PointCloudGeneration-CPSC-440-Project-
This project looks at point cloud completion using a VAE, with a focus on keeping the model simple and testing whether adding geometric features (like normals or curvature) actually improves results over using just raw point cloud data.
